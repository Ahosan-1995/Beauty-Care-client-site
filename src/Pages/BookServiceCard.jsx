

const BookServiceCard = ({allBookData}) => {

    const {service_name,price,service_image,email,provider_name,user_email,user_name,date,service_area,pending}=allBookData;

    return (
        <div>
            
        </div>
    );
};

export default BookServiceCard;